	dimension a(1)
	* , b(2) ! 续行
		  IF ((KNN(IE).EQ.M1.or.KNN(IE).EQ.M2.or.KNN(IE).EQ.M3).
	*  AND.KNN(JE).EQ.M4) THEN		!fuck
	do i = 1, 1
	
	
		if (a == 1) a(22)
		;
		if (a == 2) b(33)
		
		
	enddo
	
a = 10
end

SUBROUTINE MINTW(M,X,Y,A0,A1)  ! 有大于0限制
	IF(A1.LT.0) THEN
	A1=0.
	A0=0.
	END IF
	RETURN
END

	 SUBROUTINE MINTW2(M)
	 end SUBROUTINE